# Node
node_modules/
npm-debug.log*

# Ambiente
.env
.env.*
!.env.example
# .env.test tem só valores fixos de teste (não são segredos reais) — commitado
# de propósito pra `npm test` funcionar sem passo manual extra.
!.env.test

# Editor/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log

# Resíduo de quando os uploads ficavam em disco (agora vão pro Supabase
# Storage, ver src/utils/supabaseStorage.js) — o diretório físico ainda
# existe com arquivos de teste antigos, sem uso pelo código; mantido no
# gitignore só pra nunca acabar comitado por engano.
uploads/
